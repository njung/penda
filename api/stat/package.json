{
  "name": "stat",
  "version": "0.0.1",
  "description": "plugin to manage category",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
  }
}
